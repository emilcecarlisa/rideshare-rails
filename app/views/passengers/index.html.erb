<h2>Welcome to the best rideshare app ever - Riderly. We're revolutionizing the rideshare app industry!</h2>

  <h3>Passengers</h3>
  <ul>
    <% @passenger.each do |passenger| %>
    <li>
      <strong><%= link_to(passenger.name, passenger_path(passenger)) %></strong>
    </li>
    <% end %>
  </ul>
