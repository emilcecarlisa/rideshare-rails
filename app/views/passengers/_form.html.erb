<h3><%= page_title %></h3>

<%= render partial: "error_messages", locals: {model: @passenger} %>

<%= form_for @passenger do |p| %>
  <%= p.label :name %>
  <%= p.text_field :name %>

  <%= p.label :phone_num %>
  <%= p.text_field :phone_num %>

  <%= p.submit %>
<% end %>
