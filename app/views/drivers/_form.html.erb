<h2>
  <%= "Riderly Drivers are here for YOU." %>
</h2>

<%= form_for @driver do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :vin %>
<%= f.text_field :vin %>

<%= f.submit %>

<% end %>
